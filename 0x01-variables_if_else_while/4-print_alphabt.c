#include <stdlib.h>
#include <stdio.h>
/* Write a program that prints the alphabet in lowercase */

/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char b;
	/* for loop */
	for (b = 'a'; b <= 'z'; b++)
	{
		if (c == 'q' || c == 'e')
        {
            c++;
            continue;
        }
        else
        {
            putchar(c);
            c++;
        }
    }
putchar('\n');;
	return (0);
}
